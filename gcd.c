#include<stdio.h>
#include<conio.h>
   void main()
   {
   	int i,a,b,gcd;
   	printf("enter two numbers");
   	scanf("%d %d",&a,&b);
   	for(i=1;i<=a;i++ && i=1;i<=b;i++)
   	{
   		if(a%i==0 && b
		   %i==0);
   		printf("the gcd of %d and %d is %d",a,b,gcd);
	   }
   }
